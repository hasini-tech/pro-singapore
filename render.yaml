services:
  - type: web
    name: pro-singapore
    env: node
    plan: free
    buildCommand: pnpm install --frozen-lockfile && pnpm run build
    startCommand: node .next/standalone/server.js
    autoDeploy: true
